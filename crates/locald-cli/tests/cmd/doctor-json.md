# locald doctor --json

This test verifies that `locald doctor --json` produces valid JSON output.
The actual content varies by environment (container detection, shim status, etc.).

```console
$ locald doctor --json
...
```
