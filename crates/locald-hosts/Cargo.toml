[package]
name = "locald-hosts"
version = "0.1.0"
edition.workspace = true

[target.'cfg(unix)'.dependencies]
nix = { version = "0.30.1", features = ["fs", "ioctl", "user"] }
xattr = "1.6.1"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
