# READ-ONLY: Use 'exo' CLI to modify this file.
# Implementation Plan

[phase]
id = "113"
title = "Integration Boundaries Audit"
rfcs = ["0112", "0114"]

[plan]

[[plan.changes]]
name = "Audit optional integrations + failure modes"
type = "chore"
details = "Inventory optional integrations (Docker, CNB/buildpacks, VMM/OCI, etc) and document: detection, missing-dependency behavior, and what user-facing error/warning appears."
files = []
tests = []
satisfies = ["113.1"]
no_test_reason = "Audit + documentation step"

[[plan.changes]]
name = "Document stable/experimental integration matrix"
type = "docs"
details = "Produce a stable/experimental matrix for integrations (what’s supported, what happens when missing, remediation paths) and align docs with current behavior."
files = []
tests = []
satisfies = ["113.2"]
no_test_reason = "Documentation change"

[[plan.changes]]
name = "Ensure doctor surfaces integration availability"
type = "feat"
details = "Ensure locald doctor reports integration availability/severity consistent with the matrix (warn vs critical), so users can self-diagnose missing optional dependencies."
files = []
tests = []
satisfies = ["113.3"]
no_test_reason = "May require code changes; tests will be added once we identify the right integration-detection seams, otherwise validated via phase verification + doctor output checks."

[verification]
automated = ["Run scripts/verify-phase.sh"]
manual = []
