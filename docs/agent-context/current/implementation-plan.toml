# READ-ONLY: Use 'exo' CLI to modify this file.
# Implementation Plan

[phase]
id = "112"
title = "Dashboard Surface Audit"
rfcs = ["0112", "0114"]

[plan]

[[plan.changes]]
name = "Inventory dashboard surfaced concepts"
type = "docs"
details = "Inventory user-visible dashboard concepts (nouns, verbs, states, and navigation labels) as they exist in the UI and docs. Capture the canonical list plus where each concept appears (UI route/component and docs page). Output is a short inventory doc used as the source for vocabulary alignment."
files = []
tests = []
satisfies = ["112.1"]
no_test_reason = "Docs-only inventory"
status = "completed"

[[plan.changes]]
name = "Align dashboard vocabulary to Surface Contract"
type = "refactor"
details = "Align dashboard UI strings and docs wording to the Surface Contract (RFCs 0112/0114): rename labels, actions, and states where necessary; remove/avoid out-of-contract terms; ensure terminology is consistent across UI + docs."
files = []
tests = []
satisfies = ["112.2"]
no_test_reason = "String/vocab alignment; validated via Phase 112 flow checks"
status = "completed"

[[plan.changes]]
name = "Validate user-visible flows"
type = "test"
details = "Run and/or document the minimal flow validation for the dashboard after vocabulary alignment: confirm key pages load, status labels make sense, and primary actions match the contract. Prefer existing automation (dashboard build/lint/e2e) where available; otherwise add a small, targeted check."
files = []
tests = []
satisfies = ["112.3"]
no_test_reason = "May be manual if no stable automation exists yet"
status = "completed"

[verification]
automated = ["Run scripts/verify-phase.sh"]
manual = []
