# READ-ONLY: Use 'exo' CLI to modify this file.
# Walkthrough

[[entries]]
type = "design"
description = "Schedule follow-up to retire Docker-daemon container runtime in favor of OCI+shim"
details = "Added idea in docs/agent-context/ideas.toml: OCI container services via shim (replace legacy Docker runtime). Added Stage-0 RFC docs/rfcs/stage-0/0126-oci-container-services-via-shim.md describing consolidating container services onto locald-oci + locald-shim (libcontainer) and treating Docker daemon as legacy/optional if retained."

[[entries]]
type = "docs"
description = "Phase 113 plan synced with OCI+shim/libcontainer direction"
details = """
Marked Phase 113 tasks (113.1–113.3) complete after updating 📦 Strategy: \u001B[1msystemd\u001B[0m (PID 1 is systemd)
📦 Cleanup mode: \u001B[38;5;10menabled\u001B[39m
📦 Optional integrations:
- Docker (optional): \u001B[38;5;11munavailable\u001B[39m
  - Configured: DOCKER_HOST=unix:///var/run/docker.sock
  - Checked: /var/run/docker.sock: socket not found
  - Impact: Docker-based services will be unavailable
  - If you need this: start Docker
- Buildpacks (CNB) (optional): \u001B[38;5;11munavailable\u001B[39m
  - Dependency: requires Docker
  - Impact: buildpack-based builds will be unavailable
  - If you need this: enable Docker
- Virtualization (KVM) (optional): \u001B[38;5;10mavailable\u001B[39m
  - Checked: /dev/kvm
  - Impact: VM-based workflows enabled
✅ All critical checks passed (core).
⚠ Optional missing (non-blocking):
  - Docker
  - Buildpacks (CNB) to remove Docker optional integration checks, aligning optional integrations with privileged  (CNB) and KVM/virtualization. Updated Stage-0 Surface Contract RFC (0114) to use OCI+shim/libcontainer readiness in the integration matrix examples."""
