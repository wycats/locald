# Walkthrough - Phase 99: Demo Repository Setup

## Step 1: Color System

1. Create `examples/color-system`.
2. Create `locald.toml`.
3. Create `web/index.html`.
4. Create `api/main.py` (or similar).

## Step 2: Hydra

1. Create `examples/hydra`.
2. Create `locald.toml`.
3. Create `consent/index.html`.

## Step 3: Verification

1. Run `locald up` in each.
2. Check Dashboard.
