{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (dev host)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/locald-vscode",
        "${workspaceFolder}"
      ],
      "preLaunchTask": "Build Extension"
    }
  ]
}
